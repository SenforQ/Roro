
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let main_serviceStr:String = "getDevitag magnitude"
fileprivate let user_soundKey:String = "ceIDbuild mic app"

/*: "getFirebaseID" :*/
fileprivate let app_protectionFormat:String = "getFirmask by"
fileprivate let appSumTitle:[Character] = ["D"]

/*: "getAreaISO" :*/
fileprivate let notiInfoId:String = "always trust present globalgetAr"
fileprivate let appSubPath:[Character] = ["O"]

/*: "getProxyStatus" :*/
fileprivate let constAfterCarrierMsg:[Character] = ["g","e","t","P","r","o","x","y","S","t","a","t","u"]
fileprivate let user_toolFrameTitle:String = "drop"

/*: "getMicStatus" :*/
fileprivate let dataDateMsg:[Character] = ["g","e","t","M"]
fileprivate let userPostDecisionFormat:[Character] = ["i","c","S","t","a","t","u","s"]

/*: "getPhotoStatus" :*/
fileprivate let user_multiMsg:String = "getPhotoSappear us"
fileprivate let appDocumentFormat:String = "TATUS"

/*: "getCameraStatus" :*/
fileprivate let noti_panelStr:String = "getCamevery raw other response sound"
fileprivate let noti_toUrl:String = "asourceus"

/*: "reportAdjust" :*/
fileprivate let show_diskLocalName:String = "resr"
fileprivate let mainPhoneServicePath:String = "info"

/*: "requestLocalPush" :*/
fileprivate let k_deadlineName:String = "reqgos"
fileprivate let notiWindowFormat:[Character] = ["a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let notiDeleteTitle:[Character] = ["g"]
fileprivate let appPolicyKey:[Character] = ["e","t","L","a","n","g","C","o","d","e"]

/*: "getTimeZone" :*/
fileprivate let constValueKey:[Character] = ["g","e"]
fileprivate let showDisplayName:String = "tTimtool count root"

/*: "getInstalledApps" :*/
fileprivate let showFileMessage:String = "getIi root"
fileprivate let userFinishValue:String = "edAppsguard any version mirror list"

/*: "getSystemUUID" :*/
fileprivate let data_multiFormat:String = "scale spacegetS"
fileprivate let data_bodyId:String = "activityD"

/*: "getCountryCode" :*/
fileprivate let user_behaviorName:[Character] = ["g","e","t","C"]
fileprivate let userTransformMsg:String = "oprogressntry"

/*: "inAppRating" :*/
fileprivate let kCoreValue:[Character] = ["i","n"]
fileprivate let main_contentDeleteMessage:String = "AppRabehavior sound"

/*: "apPay" :*/
fileprivate let data_bounceStr:String = "apPayscale app return scene layer"

/*: "subscribe" :*/
fileprivate let constNumberMsg:[Character] = ["s"]
fileprivate let app_displayData:[Character] = ["u","b","s","c","r","i","b","e"]

/*: "openSystemBrowser" :*/
fileprivate let constOptionBridgeValue:String = "s soundopenSy"
fileprivate let show_needSchemeTitle:String = "scaleowsescale"

/*: "closeWebview" :*/
fileprivate let appTunDropName:[Character] = ["c","l","o","s","e","W","e","b"]
fileprivate let data_indicatorUrl:String = "viesuccess"

/*: "openNewWebview" :*/
fileprivate let mainOriginalValue:[Character] = ["o"]
fileprivate let app_challengeMessage:String = "penNewclose server sub deadline"

/*: "reloadWebview" :*/
fileprivate let showToolTitle:String = "reloadWprotection drop screen"
fileprivate let user_kindMsg:String = "EBVIEW"

/*: "typeName" :*/
fileprivate let showGrantText:[UInt8] = [0x81,0x8c,0x85,0x90,0xbb,0x94,0x98,0x90]

private func tutelageTrack(session num: UInt8) -> UInt8 {
    return num ^ 245
}

/*: "deviceID" :*/
fileprivate let main_countryOriginalMessage:[UInt8] = [0x26,0x27,0x38,0x2b,0x25,0x27,0xb,0x6]

fileprivate func styleWait(root num: UInt8) -> UInt8 {
    let value = Int(num) - 194
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fireBaseID" :*/
fileprivate let const_promptId:[UInt8] = [0x40,0x4f,0x54,0x43,0x64,0x47,0x55,0x43,0x6f,0x62]

private func nameSpace(observer num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "areaISO" :*/
fileprivate let showBackTopValue:[UInt8] = [0x17,0x4,0x13,0x17,0x3f,0x25,0x39]

/*: "isProxy" :*/
fileprivate let main_currencyData:[UInt8] = [0x9a,0x80,0xa3,0x81,0x9c,0x8b,0x8a]

/*: "langCode" :*/
fileprivate let k_byInputTitle:[UInt8] = [0x77,0x7a,0x75,0x7c,0x58,0x74,0x7f,0x7e]

private func nativePrivacy(space num: UInt8) -> UInt8 {
    return num ^ 27
}

/*: "timeZone" :*/
fileprivate let constDisabledData:[UInt8] = [0x37,0x2c,0x30,0x28,0x1d,0x32,0x31,0x28]

fileprivate func appearFlexible(available num: UInt8) -> UInt8 {
    let value = Int(num) - 195
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "installedApps" :*/
fileprivate let noti_neverMessage:[UInt8] = [0x2b,0x2c,0x31,0x36,0x23,0x2e,0x2e,0x27,0x26,0x3,0x32,0x32,0x31]

private func padRevenue(zone num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "systemUUID" :*/
fileprivate let k_protectionContent:[UInt8] = [0x50,0x5a,0x50,0x57,0x46,0x4e,0x76,0x76,0x6a,0x67]

/*: "countryCode" :*/
fileprivate let dataBucketName:[UInt8] = [0x19,0x15,0xf,0x14,0xe,0x8,0x3,0x39,0x15,0x1e,0x1f]

/*: "status" :*/
fileprivate let dataFatalId:[UInt8] = [0x60,0x67,0x72,0x67,0x66,0x60]

/*: "isAuth" :*/
fileprivate let const_labSignalText:[UInt8] = [0x9b,0xa5,0x73,0xa7,0xa6,0x9a]

fileprivate func dredgingBucket(response num: UInt8) -> UInt8 {
    let value = Int(num) + 206
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isFirst" :*/
fileprivate let dataCloseContent:[UInt8] = [0x4b,0x55,0x28,0x4b,0x54,0x55,0x56]

fileprivate func windowWhite(fire num: UInt8) -> UInt8 {
    let value = Int(num) + 30
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let userColorTriggerText:String = "_"
fileprivate let dataProductionActionTransportValue:String = "for multi signal_Loca"

/*: "identifier" :*/
fileprivate let noti_disappearTailRemoteKey:[UInt8] = [0xee,0xe9,0xea,0xf3,0xf9,0xee,0xeb,0xee,0xea,0xf7]

fileprivate func stopLanguage(script num: UInt8) -> UInt8 {
    let value = Int(num) + 123
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "HTTPProxy" :*/
fileprivate let notiSameValue:[Character] = ["H","T","T","P","P"]
fileprivate let mainAccessIndexRatingData:[Character] = ["r","o","x","y"]

/*: "HTTPSProxy" :*/
fileprivate let user_cancelKey:[Character] = ["H","T","T","P","S","P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let user_successStr:[Character] = ["S","O","C","K"]
fileprivate let constTitleData:String = "SProxyproduct the background device"

/*: "__SCOPED__" :*/
fileprivate let constChallengePath:String = "fatal kit your dismiss app__SC"
fileprivate let data_fileKey:String = "category agentOPED__"

/*: "tap" :*/
fileprivate let mainStorageValue:String = "methodp"

/*: "tun" :*/
fileprivate let show_reportFormat:String = "tre"

/*: "ipsec" :*/
fileprivate let k_schemeTitle:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let mainVisibleMessage:String = "versionp"

/*: "Retry After or Go to " :*/
fileprivate let main_barText:String = "number progress guardRetr"
fileprivate let noti_transactionId:String = "input cleary Aft"
fileprivate let constTunData:String = "Go to available no now"

/*: "Feedback" :*/
fileprivate let mainObserverName:String = "network vertical info challenge countryFeedbac"
fileprivate let mainUsTitle:String = "zone"

/*: " to contact us" :*/
fileprivate let noti_dataValue:String = " to ctransport network warn network at"
fileprivate let kFeedbackData:String = "access handle for forct u"
fileprivate let app_listMessage:[Character] = ["s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MobileConstraintSpeed.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let appComplianceAssertKey = (String(main_serviceStr.prefix(7)) + String(user_soundKey.prefix(4))) // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let const_textMessage = (String(app_protectionFormat.prefix(6)) + "ebaseI" + String(appSumTitle)) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let k_toTitle = (String(notiInfoId.suffix(5)) + "eaIS" + String(appSubPath)) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let app_messageText = (String(constAfterCarrierMsg) + user_toolFrameTitle.replacingOccurrences(of: "drop", with: "s")) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let dataSameKey = (String(dataDateMsg) + String(userPostDecisionFormat)) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let k_rejectFormat = (String(user_multiMsg.prefix(9)) + appDocumentFormat.lowercased()) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let dataAllowName = (String(noti_panelStr.prefix(6)) + "eraSt" + noti_toUrl.replacingOccurrences(of: "source", with: "t")) // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let userCountervalStr = (show_diskLocalName.replacingOccurrences(of: "s", with: "po") + "tAdjus" + mainPhoneServicePath.replacingOccurrences(of: "info", with: "t")) // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let showPackageMessage = (k_deadlineName.replacingOccurrences(of: "go", with: "ue") + "tLoc" + String(notiWindowFormat)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let dataStatusMultiUrl = (String(notiDeleteTitle) + String(appPolicyKey)) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let showBucketStr = (String(constValueKey) + String(showDisplayName.prefix(4)) + "eZone") // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let userRatingData = (String(showFileMessage.prefix(4)) + "nstall" + String(userFinishValue.prefix(6))) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let main_installTitle = (String(data_multiFormat.suffix(4)) + "ystemUU" + data_bodyId.replacingOccurrences(of: "activity", with: "I")) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let data_adKey = (String(user_behaviorName) + userTransformMsg.replacingOccurrences(of: "progress", with: "u") + "Code") // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let k_buildCameraSumMsg = (String(kCoreValue) + String(main_contentDeleteMessage.prefix(5)) + "ting") // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let show_backgroundValue = (String(data_bounceStr.prefix(5))) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let dataMultiPath = (String(constNumberMsg) + String(app_displayData)) // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let user_allowKey = (String(constOptionBridgeValue.suffix(6)) + "stemB" + show_needSchemeTitle.replacingOccurrences(of: "scale", with: "r")) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let appPositiveContent = (String(appTunDropName) + data_indicatorUrl.replacingOccurrences(of: "success", with: "w")) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let const_lostEndStr = (String(mainOriginalValue) + String(app_challengeMessage.prefix(6)) + "Webview") // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let mainScaleStr = (String(showToolTitle.prefix(7)) + user_kindMsg.lowercased()) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct EndowTransformable: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: NetMeasurable?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct NetMeasurable: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension FilterDelegate {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func relic(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = EndowTransformable.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case appComplianceAssertKey:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = MenuBridgeCount.appBy()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: main_countryOriginalMessage.map{styleWait(root: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case const_textMessage:
                //: AppWebViewController.getFireBaseToken { str in
                FilterDelegate.openeTime { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_promptId.map{nameSpace(observer: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case k_toTitle:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = FilterDelegate.instance()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: showBackTopValue.map{$0^118}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case app_messageText:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = FilterDelegate.observer()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: main_currencyData.map{$0^243}, encoding: .utf8)!: status])

            //: case getLangCode:
            case dataStatusMultiUrl:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_byInputTitle.map{nativePrivacy(space: $0)}, encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case showBucketStr:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: constDisabledData.map{appearFlexible(available: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case userRatingData:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_neverMessage.map{padRevenue(zone: $0)}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case main_installTitle:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_protectionContent.map{$0^35}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case data_adKey:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: dataBucketName.map{$0^122}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case k_buildCameraSumMsg:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                FilterDelegate.start()

            //: case apPay:
            case show_backgroundValue:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.revenue(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: dataFatalId.map{$0^19}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case dataMultiPath:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.revenue(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: dataFatalId.map{$0^19}, encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case user_allowKey:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case appPositiveContent:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.signal()

            //: case openNewWebview:
            case const_lostEndStr:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    FilterDelegate.data(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case mainScaleStr:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.pullUp()

            //: case getMicStatus:
            case dataSameKey:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                RetainedEventBehavior.shared.group { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_labSignalText.map{dredgingBucket(response: $0)}, encoding: .utf8)!: auth, String(bytes: dataCloseContent.map{windowWhite(fire: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case k_rejectFormat:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                RetainedEventBehavior.shared.pendingCenter { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_labSignalText.map{dredgingBucket(response: $0)}, encoding: .utf8)!: auth, String(bytes: dataCloseContent.map{windowWhite(fire: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case dataAllowName:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                RetainedEventBehavior.shared.sumry { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName, String(bytes: const_labSignalText.map{dredgingBucket(response: $0)}, encoding: .utf8)!: auth, String(bytes: dataCloseContent.map{windowWhite(fire: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case userCountervalStr:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        MenuBridgeCount.modifyExecuteAdd(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        MenuBridgeCount.productPending(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case showPackageMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: showGrantText.map{tutelageTrack(session: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                FilterDelegate.everyBridge(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension FilterDelegate {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func data(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = FilterDelegate()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        LastGestureDirection.viewMethod()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func everyBridge(_ model: EndowTransformable) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(constComplianceStr)" + (userColorTriggerText.capitalized + String(dataProductionActionTransportValue.suffix(5)) + "lPush")
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: noti_disappearTailRemoteKey.map{stopLanguage(script: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func openeTime(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func instance() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func observer() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if FilterDelegate.agent() || FilterDelegate.recordVpn() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func agent() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(notiSameValue) + String(mainAccessIndexRatingData))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(user_cancelKey))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(user_successStr) + String(constTitleData.prefix(6)))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func recordVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(constChallengePath.suffix(4)) + String(data_fileKey.suffix(6)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((mainStorageValue.replacingOccurrences(of: "method", with: "ta"))) || keyStr.contains((show_reportFormat.replacingOccurrences(of: "re", with: "un"))) || keyStr.contains((String(k_schemeTitle))) || keyStr.contains((mainVisibleMessage.replacingOccurrences(of: "version", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func start() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    private func decorationViaMemento() {
        DimensionByCycle.add((String(main_barText.suffix(4)) + String(noti_transactionId.suffix(5)) + "er or " + String(constTunData.prefix(6))) + "\"" + (String(mainObserverName.suffix(7)) + mainUsTitle.replacingOccurrences(of: "zone", with: "k")) + "\"" + (String(noti_dataValue.prefix(5)) + "onta" + String(kFeedbackData.suffix(4)) + String(app_listMessage)))
    }
    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func revenue(productId: String, source: Int = -1, payType: TotalerdependType, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        DimensionByCycle.windowEmpty()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        MicManager.shared.drop(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            DimensionByCycle.isColor()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    self.decorationViaMemento()
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.accessGlobal()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
